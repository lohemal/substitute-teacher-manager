# 보결 배정 시스템 — 설계안 v1

작성일: 2026-09-04 / 검토 완료 후 구현 착수

> **실제 스키마의 원본은 `src-tauri/migrations/001_init.sql`입니다.**
> 이 문서는 설계 의도를 설명하며, 아래 항목은 검토 결과를 반영해 문서보다 확장되었습니다.
>
> | 항목 | 반영 내용 |
> |---|---|
> | `terms` | `school_year`, `semester`, `is_current` 추가. 최초 설정에서 현재 학년도·학기를 지정하고, 이후 화면은 현재 학기만 다룸. 이전 학기 보결 기록은 삭제하지 않고 조회 가능 |
> | `lessons` | `lesson_type`(SPECIAL/CROSS/CO/ETC)과 `replaces_homeroom` 추가. **공동수업은 담임도 함께 들어가므로 `replaces_homeroom=0`** — 담임 시간표 계산에서 제외하지 않음 |
> | `teacher_busy_blocks` | `block_type`(DUTY 고정업무 / NO_SUB 보결 배정 불가 / OTHER) 추가 |
> | `absences` | 종일·일부 시간 부재를 모두 표현(`is_all_day`, `start_min`, `end_min`) + CHECK 제약 |
> | `substitutions` | 같은 교사 이중 배정, 같은 학급 이중 보결을 막는 부분 유니크 인덱스 2개 추가 |
> | 조회 결과 | `TargetSlot.in_charge`(이 시간을 원래 담당하는 교사) + `FindResult.notice` 추가. **그 시간에 전담이 들어오면 담임이 결근해도 보결이 필요 없으므로** "전담(과목) 시간입니다. 확인 바랍니다." 안내를 조회 전·후에 띄워 헛배정을 막음. 전담 본인이 결근한 경우에는 안내하지 않음 |
> | 배정 (Phase 8) | 스키마 변경 없이 001/002의 컬럼을 그대로 쓴다. 저장 값은 모두 **그 시점 스냅샷** — `term_id`·`date`·`day_of_week`·`grade`·`class_id`·`class_label`·`slot_type`·`period_no`·`slot_label`·`start_min`·`end_min`·`absent_teacher_id`·`sub_teacher_id`·`subject_id`·`reason_code`·`status`·`created_at`·`cancelled_at`·`cancel_reason`. 취소는 `status='CANCELLED'`로만 바꾸고 행은 지우지 않는다 |
> | 교사명 스냅샷 | **넣지 않기로 했다.** Phase 4에서 보결 기록이 있는 교사는 삭제가 막히고 비활성만 되므로 `teacher_id`가 항상 풀린다. 이름이 바뀌는 경우는 대부분 오타 수정·개명이라 과거 기록도 새 이름으로 보이는 편이 옳다. 반 이름은 반대다 — 학년마다 새로 붙는 값이므로 `class_label`로 고정한다 |
> | 겹침 방지 | 부분 유니크 인덱스는 **같은 시작 시각**만 막는다. SQLite로 구간 겹침을 제약할 수 없으므로 쓰기 트랜잭션 안에서 `domain::assign::verify`(조회와 같은 엔진) + `guard_overlap`(SQL 구간 질의)으로 확인한다. 일괄 배정은 한 칸을 확정할 때마다 `occupy`로 하루치 자료에 반영해 다음 칸을 검증하므로, 같은 교사를 연속 교시에 넣는 것은 되고 같은 시각 두 반에 넣는 것은 막힌다 |
> | 중간놀이 | **보결을 배정하지 않는다.** 현장에서 그 시간까지 사람을 넣지는 않기 때문이다. 다만 후보 판정에서는 여전히 담임의 '바쁜 시간'이므로, 중간놀이 중인 선생님을 다른 반 보결로 부르지는 않는다 |
> | 다건 배정 화면 | 같은 선생님이 한 반의 여러 교시에 들어가는 것은 현장에서 흔하므로 **막지도, 자동으로 다음 순위로 넘기지도 않는다.** 각 칸은 늘 추천 1순위를 골라 두고, 같은 분이 두 곳 이상이면 상태 칸에 연한 빨강 `중복`으로만 표시한다. 시각까지 겹치면 저장이 불가능하므로 `시각 겹침`으로 따로 구분하고 [배정]을 잠근다 |
> | 현황 (Phase 9) | **집계표를 두지 않는다.** 모든 숫자는 조회할 때마다 `substitutions`·`absences`를 다시 세어 만든다. 그래서 배정을 취소하거나 고치면 곧바로 반영되고, 어긋날 여지가 없다 |
> | '보결 필요' 계산 | 등록된 결근에서 `domain::assign::duty_slots`로 뽑는다 — 다건 배정 화면과 **같은 계산**이라 두 화면의 숫자가 어긋나지 않는다. 결근이 없는 날은 건너뛰므로 한 달치를 봐도 실제로 읽는 날은 몇 날뿐이다. `required = covered + unassigned`는 항상 성립하지만, `assigned`(기간 안 배정 건수 전체)는 결근 등록 없이 배정한 건을 포함하므로 `covered`보다 클 수 있다 |
> | 공정성 표시 | `domain::fairness`. **같은 구분(담임·전담·기타)끼리만** 견주고, 평균에서 2회 또는 평균의 25% 가운데 큰 폭을 벗어날 때만 `평균보다 많음/적음`을 붙인다. 구분 안에 3명 미만이거나 모두 같으면 표시하지 않는다. '과다·문제' 같은 판정 문구는 쓰지 않고 색도 경고색을 피한다 |
> | 백업 · 복원 (Phase 10) | 파일을 복사하지 않고 **SQLite 백업 API**를 쓴다 — WAL에만 있는 내용까지 담기고 쓰는 도중에도 안전하다. 복원은 ① 지금 자료를 `before-restore-…`로 백업 → ② 연결을 닫고 → ③ 백업 내용을 자료 파일로 옮기고 → ④ 다시 열며 마이그레이션 적용. 어느 단계에서 실패해도 ①로 되돌아간다. 파일은 열어서 스키마 버전과 필수 표를 확인한 뒤에만 복원한다 |
> | 자동 백업 | `OFF / DAILY / ON_EXIT`, 기본 DAILY·10개 보관. **자동 백업만** 개수를 지켜 지우고 직접 만든 백업(`보결자료-…`)과 안전 백업(`before-…`)은 건드리지 않는다 |
> | 동작 옵션 | `repo::settings::ENGINE_OPTIONS` 한 곳에만 정의한다 (화면은 이 목록을 보고 그린다). 새 옵션은 여기 한 줄 + `EngineSettings` 한 필드. **시간 충돌을 끄는 옵션은 만들지 않는다** — 방침이 아니라 사실의 문제이고, 끄는 순간 한 사람이 같은 시각에 두 교실에 있게 된다 |
> | 새 학기 | `terms` 행을 새로 만들고 학급·시정표·전담 수업을 **선택적으로 복사**한다. 교사·과목은 term_id가 없어 학교에 속하므로 복사할 것이 없다. 지난 학기 자료는 손대지 않으며, 보결 기록은 `class_label`·시각 스냅샷 덕분에 새 학기에 반 이름이 바뀌어도 그대로다 |
> | 전담 식사시간 | 학년별 시정표의 점심 구간에서 뽑는다. 일반 보결에만 걸리고 **점심 보결에는 걸리지 않는다** (§5.4-B) |
> | 엑셀(XLSX) | 화면에 걸린 기간·필터를 그대로 넘겨 내보낸다. **숫자는 숫자로** 저장하므로 엑셀에서 바로 합계를 낼 수 있고, 한글은 형식 자체가 UTF-8이라 깨지지 않는다. 표가 여러 개면 시트로 나눈다 (§5.6) |
> | 초기화 | 동작 옵션 초기화(자료 유지)와 자료 전체 초기화를 완전히 분리했다. 후자는 `전체 초기화` 문구를 그대로 입력해야 하고, 지우기 직전에 반드시 백업을 남긴다 |
> | 업데이트 (Phase 11) | Tauri Updater + GitHub Releases. 앱 안에 **공개 키**가 박혀 있고, 개인 키로 서명된 파일만 설치한다. 확인 → 내려받기 → 다시 시작을 모두 사람이 누르게 한다 (업무 중 갑자기 다시 켜지면 안 된다). 개인 키는 저장소 밖(`%USERPROFILE%.bogyeol-release`)에 두고 GitHub Secret 으로만 CI에 넘긴다. 자세한 절차는 `docs/02-배포.md` |
> | 버전 | `package.json`·`tauri.conf.json`·`Cargo.toml` 세 곳이 같아야 업데이터가 새 버전을 알아본다. 손으로 고치지 않고 `npm run version:set 0.2.0` 을 쓴다 |
>
> 예외 일정(교차수업·공동수업·고정업무·보결 불가·기타 정기 일정)은 **등록하지 않으면 아무 영향이 없습니다.**
> 일반 학교는 전담 시간표만 입력하면 되고, 필요한 학교만 예외를 추가합니다.

---

## 0. 요구사항 검토 의견 (먼저 확인이 필요한 부분)

설계에 앞서, 요구사항을 그대로 구현했을 때 문제가 되거나 크게 개선할 수 있는 지점입니다.

### (0) 이 프로그램은 초등학교용이다 (v0.1.7)

학교급을 묻지 않습니다. 새로 설정하면 `ELEMENTARY` 로 저장됩니다.

중·고등학교를 넣지 않은 것은 만들다 만 것이 아니라 **운영 구조가 다르기
때문**입니다. 중·고등학교는 교사마다 담당 교과가 있고, 대부분이 개인
시간표를 가지며, 같은 교과 교사의 보강 우선순위·교환수업과 보강의 관계·결강
누계·주간 시수 같은 **학교별 규정**이 순서에 영향을 줍니다. 초등학교용 판정을
그대로 적용하면 그럴듯하지만 **틀린 답**이 나옵니다.

그래서 이렇게 했습니다.

| 무엇 | 어떻게 |
|---|---|
| 화면 | 학교급 선택 칸이 없다 (`src/lib/schoolType.ts` 의 `CURRENT_SCHOOL_TYPE` 로 저장) |
| 타입·DB | `SchoolType` 세 값과 `school_type` 의 `ELEMENTARY \| MIDDLE \| HIGH` 를 **그대로 둔다** |
| 학년 범위 | 학교급별 기본 학년 범위도 그대로 둔다 |
| 중·고 자료를 열면 | 값을 바꾸지 않고 **보결 조회만 막는다** (`src/pages/FindPage.tsx`) |

마지막 줄이 중요합니다. 예전 자료나 시험용 자료가 중·고등학교로 저장되어
있을 때, 조용히 초등학교 기준으로 계산해 답을 내놓는 것은 자료를 그대로
두는 것보다 나쁩니다. 기록 열람·현황·수당·백업은 그대로 쓸 수 있습니다.

나중에 중·고등학교 운영 방식을 조사해 지원하기로 하면 위 네 줄에서부터
넓히면 됩니다. 시간 겹침 판정·결근·배정·통계·수당 같은 공통 부분은 그대로
쓸 수 있습니다.

### (1) 담임 시간표는 입력받지 않고 "파생"시킨다 — 가장 중요한 결정

요구사항에는 전담교사 시간표만 입력하도록 되어 있습니다. 그렇다면 담임교사가 언제 수업 중인지는
어떻게 아는가? 답은 **초등학교 구조상 계산이 가능하다**는 것입니다.

> 담임의 수업 시간 = (자기 반의 그 요일 전체 교시) − (그 반에 전담이 들어오는 교시)

즉 5-2반 수요일에 1~6교시가 있고 3교시에 전담 영어가 들어온다면,
5-2 담임은 1, 2, 4, 5, 6교시 수업 / 3교시 공강입니다.

이 방식의 장점:

- 담임 20여 명의 시간표를 일일이 입력할 필요가 없음 → 초기 설정 시간이 크게 줄어듦
- 전담 시간표만 정확하면 전체가 정확해짐 (입력 지점이 하나 = 오류 지점이 하나)
- 전담 시간표를 수정하면 담임 공강이 자동으로 따라 바뀜

예외 처리:

- 담임이 다른 반 수업에 들어가는 경우(교과 교차 지도) → 담임도 전담과 동일한 테이블에
  행을 추가할 수 있게 함 (테이블명을 `lessons`로 두고 교사 구분과 무관하게 사용)
- 특정 교사의 고정 업무(순회, 협의회 등) → `teacher_busy_blocks`로 별도 등록

**설정에서 `담임 시간표 자동 계산` 옵션을 켜고 끌 수 있게 하되, 기본값은 켜짐으로 합니다.**

### (2) "몇 교시까지 수업하는가"는 별도 테이블로 두지 않는다

시정표에 해당 요일의 교시 행이 존재하는지 여부가 곧 "그 날 몇 교시까지인가"입니다.
수요일 1학년에 1~4교시 행만 있으면 1학년은 수요일 4교시까지입니다.
별도 컬럼을 두면 두 데이터가 어긋날 수 있으므로 단일 출처로 관리합니다.

### (3) 시정표는 "학년별"이 아니라 "프로필 + 학년 매핑"으로 관리한다

학년 6개 × 요일 5개 × 슬롯 8개 = 240행을 손으로 입력하는 건 현실적이지 않습니다.
실제 학교는 보통 2~3개 패턴만 씁니다(저학년 / 중학년 / 고학년).

```
시정표 프로필: [저학년 시정표] [중학년 시정표] [고학년 시정표]
학년 매핑:     1학년→저학년, 2학년→저학년, 3학년→중학년, … 6학년→고학년
```

프로필 1개만 만들어 전 학년에 매핑하면 "학교 전체 하나의 시정표"도 그대로 됩니다.
입력 화면에서는 여전히 "1학년 월요일 5교시는 몇 시?"로 보이게 하고, 저장만 프로필 단위로 합니다.

### (4) 시간은 전부 `자정 기준 분(0~1439) 정수`로 저장한다

`"09:00"` 같은 문자열로 저장하면 비교·차집합·중복 검사에서 반드시 사고가 납니다.
DB에는 `start_min=540, end_min=580`으로 저장하고 화면에서만 `09:00`으로 변환합니다.
겹침 판정도 정수 비교 한 줄로 끝납니다.

### (5) 보결 기록에는 "그때의 실제 시각"을 스냅샷으로 박아 넣는다

학기 중 시정표가 바뀌면 과거 기록의 "5학년 6교시"가 다른 시각을 가리키게 됩니다.
`substitutions` 테이블에 `start_min`, `end_min`, 학년, 반, 교시 라벨을
**비정규화하여 복사 저장**해 과거 기록이 절대 변하지 않도록 합니다.

### 추가로 제안하는 항목 (요구사항에 없지만 필요)

| 항목 | 이유 |
|---|---|
| **결근(부재) 기록 테이블** | A교사가 하루 종일 출장이면 A는 다른 시간 보결 후보에서도 빠져야 합니다. 결근교사 1명만 제외하는 로직으로는 처리 불가 |
| **하루 일괄 보결** | 실제 업무는 "김OO 선생님 내일 하루 연가 → 1~6교시 전부 보결"입니다. 6번 조회는 비효율. 한 화면에서 교시별 추천을 한 번에 보고 배정 |
| **학년도/학기(term) 개념** | 다음 학기에 시간표를 새로 짤 때 기존 데이터를 지우지 않고 세트를 갈아끼움. 과거 통계 보존 |
| **백업/복원** | 로컬 SQLite 단일 파일. PC 교체·고장 대비. 설정 화면에 [백업 파일 내보내기] |

---

## 1. 전체 시스템 아키텍처

### 1.1 계층 구조

```
┌─────────────────────────────────────────────────────────┐
│  React UI (TypeScript)                                   │
│  ─ pages/      화면 단위 (보결조회, 배정내역, 교사관리…)   │
│  ─ features/   화면별 컴포넌트 + 훅                       │
│  ─ components/ 공용 UI (Button, Card, Table, Stepper…)   │
│  ─ styles/     디자인 토큰 (하늘색 / 연두색 / 흰색)        │
└───────────────┬─────────────────────────────────────────┘
                │  TanStack Query (캐시·무효화)
┌───────────────▼─────────────────────────────────────────┐
│  src/ipc/*.ts   타입 안전 IPC 래퍼 (얇은 함수 모음)        │
│  src/types/*.ts Rust와 1:1 대응하는 DTO 타입              │
└───────────────┬─────────────────────────────────────────┘
                │  Tauri invoke (JSON)
┌───────────────▼─────────────────────────────────────────┐
│  Rust (src-tauri)                                        │
│  ─ commands/   IPC 진입점. 검증 + DTO 변환만              │
│  ─ domain/     ★ 순수 로직 (DB·Tauri 의존 없음)           │
│      ├ time.rs        구간 / 겹침 계산                    │
│      ├ schedule.rs    교사별 일정(BusyBlock) 생성          │
│      ├ exclusion.rs   STEP1 후보 필터링 (룰 레지스트리)    │
│      └ priority.rs    STEP2 추천 정렬  (룰 레지스트리)     │
│  ─ repo/       SQL 전담 (rusqlite)                       │
│  ─ db/         커넥션, 마이그레이션 러너                   │
│  ─ error.rs    사용자용 한국어 메시지 매핑                 │
└───────────────┬─────────────────────────────────────────┘
                │
        ┌───────▼────────┐
        │ SQLite (로컬)   │  %APPDATA%\bogyeol\bogyeol.db
        └────────────────┘
```

### 1.2 핵심 설계 원칙

**보결 판단 엔진은 Rust `domain/`에 둔다.**

- `cargo test`로 UI 없이 수십 개의 시간 충돌 케이스를 자동 검증 가능.
  이 프로그램에서 가장 틀리면 안 되는 부분이기 때문
- DB·Tauri에 의존하지 않는 순수 함수 → 입력(교사목록, 시정표, 수업, 기존보결)만 주면 결과가 결정됨
- 프론트에서 같은 로직을 재구현할 필요가 없음(중복 구현 = 불일치)

**`domain/`은 DB를 모른다.** `repo/`가 필요한 데이터를 전부 읽어 구조체로 넘기고,
`domain/`은 그것만 보고 계산합니다. 그래서 테스트에서 DB 없이 가짜 데이터로 검증할 수 있습니다.

### 1.3 기술 선택

| 항목 | 선택 | 비고 |
|---|---|---|
| 빌드 | Vite + React 18 + TS strict | |
| DB 접근 | `rusqlite` (bundled SQLite) | `tauri-plugin-sql` 대신 직접 사용. 트랜잭션·마이그레이션 통제, 비즈니스 로직을 Rust에 두기 위함 |
| 마이그레이션 | `PRAGMA user_version` + 번호순 SQL 파일 | 외부 크레이트 없이 단순하게 |
| 상태관리 | TanStack Query + Zustand(최소) | 서버(=DB) 상태는 Query, UI 상태만 Zustand |
| 라우팅 | React Router (HashRouter) | |
| 스타일 | CSS Modules + CSS 변수 토큰 | 무거운 UI 프레임워크 없이 가볍게 |
| 날짜 | `dayjs`(ko) / Rust `chrono` | |
| 테스트 | `cargo test`(도메인), Vitest(유틸) | |

### 1.4 파일 구조

```
보결 배정 시스템/
├─ docs/01-설계안.md
├─ src/                        # React
│   ├─ main.tsx  App.tsx  router.tsx
│   ├─ pages/
│   │   ├─ Welcome/            # 최초 실행 화면
│   │   ├─ Setup/              # 설정 마법사 (Step 1~6)
│   │   ├─ Find/               # ★ 보결 조회 (메인)
│   │   ├─ Assignments/        # 배정 내역
│   │   ├─ Stats/              # 보결 현황
│   │   ├─ Teachers/           # 교사 관리
│   │   ├─ Timetable/          # 시간표 관리
│   │   └─ Settings/           # 설정
│   ├─ features/  components/  ipc/  types/  lib/  styles/
└─ src-tauri/
    ├─ tauri.conf.json  Cargo.toml
    ├─ migrations/001_init.sql
    └─ src/
        ├─ main.rs  lib.rs  error.rs
        ├─ db/{mod.rs, migrate.rs}
        ├─ repo/{school,teacher,bell,lesson,sub,settings}.rs
        ├─ domain/{time,schedule,exclusion,priority,mod}.rs
        └─ commands/{setup,teacher,bell,lesson,find,sub,stats,backup}.rs
```

---

## 2. SQLite DB Schema

전체 SQL은 `src-tauri/migrations/001_init.sql`로 들어갑니다.

### 2.1 메타 / 설정

```sql
CREATE TABLE app_meta (               -- 단일 key-value
  key   TEXT PRIMARY KEY,             -- schema_version, app_version,
  value TEXT NOT NULL,                -- last_update_check_at, setup_completed_at
  updated_at TEXT NOT NULL
);

CREATE TABLE settings (               -- 동작 옵션
  key        TEXT PRIMARY KEY,
  value_json TEXT NOT NULL,
  updated_at TEXT NOT NULL
);
-- 기본값
--  derive_homeroom_schedule       : true   담임 시간표 자동 계산
--  exclude_homeroom_on_own_lunch  : true   자기 학년 점심시간에 담임 제외
--  exclude_absent_teacher_all_day : true   결근 교사는 그날 전체 제외
--  show_excluded_teachers         : true   제외된 교사도 사유와 함께 표시
--  update_check_interval_hours    : 24

CREATE TABLE setup_steps (            -- 설정 마법사 진행 상태
  step_key   TEXT PRIMARY KEY,        -- SCHOOL|BELL|TEACHER|LESSON|PRIORITY|DONE
  step_order INTEGER NOT NULL,
  status     TEXT NOT NULL,           -- PENDING | IN_PROGRESS | DONE
  updated_at TEXT NOT NULL
);
```

### 2.2 학교 / 학기 / 학급

```sql
CREATE TABLE school (
  id INTEGER PRIMARY KEY CHECK (id = 1),
  name        TEXT    NOT NULL,
  school_type TEXT    NOT NULL DEFAULT 'ELEMENTARY',
  min_grade   INTEGER NOT NULL DEFAULT 1,
  max_grade   INTEGER NOT NULL DEFAULT 6,
  school_days TEXT    NOT NULL DEFAULT '1,2,3,4,5',   -- 1=월 … 7=일
  created_at TEXT, updated_at TEXT
);

CREATE TABLE terms (                  -- 학년도/학기: 시간표 세트를 묶는 단위
  id INTEGER PRIMARY KEY,
  name       TEXT NOT NULL,           -- '2026학년도 2학기'
  start_date TEXT, end_date TEXT,
  is_active  INTEGER NOT NULL DEFAULT 0
);
-- Phase 1~9에서는 기본 term 1개만 사용하며 UI에 노출하지 않음.
-- 후에 '새 학기 시작하기'(현재 세트 복제) 기능만 추가하면 됨.

CREATE TABLE classes (
  id INTEGER PRIMARY KEY,
  term_id  INTEGER NOT NULL REFERENCES terms(id) ON DELETE CASCADE,
  grade    INTEGER NOT NULL,
  class_no INTEGER NOT NULL,
  homeroom_teacher_id INTEGER REFERENCES teachers(id) ON DELETE SET NULL,
  active   INTEGER NOT NULL DEFAULT 1,
  UNIQUE (term_id, grade, class_no)
);
```

### 2.3 교사 / 과목

```sql
CREATE TABLE teacher_roles (          -- 코드 테이블 (학교가 추가 가능)
  code       TEXT PRIMARY KEY,        -- HOMEROOM | SPECIAL | OTHER
  label      TEXT NOT NULL,           -- 담임 | 전담 | 기타
  sort_order INTEGER NOT NULL,
  is_builtin INTEGER NOT NULL DEFAULT 1
);

CREATE TABLE teachers (
  id INTEGER PRIMARY KEY,
  name             TEXT    NOT NULL,
  role_code        TEXT    NOT NULL REFERENCES teacher_roles(code),
  is_substitutable INTEGER NOT NULL DEFAULT 1,   -- ★ 보결 배정 대상 여부
  memo             TEXT,                         -- '교감', '보건' 등
  sort_order       INTEGER NOT NULL DEFAULT 0,
  active           INTEGER NOT NULL DEFAULT 1,   -- 퇴직/전출 시 0 (기록 보존)
  created_at TEXT, updated_at TEXT
);

CREATE TABLE subjects (
  id INTEGER PRIMARY KEY,
  name   TEXT NOT NULL UNIQUE,
  active INTEGER NOT NULL DEFAULT 1
);

CREATE TABLE teacher_subjects (       -- N:M — 한 교사가 여러 과목 담당 가능
  teacher_id INTEGER NOT NULL REFERENCES teachers(id) ON DELETE CASCADE,
  subject_id INTEGER NOT NULL REFERENCES subjects(id) ON DELETE CASCADE,
  is_primary INTEGER NOT NULL DEFAULT 0,
  PRIMARY KEY (teacher_id, subject_id)
);
```

담임 배정은 `classes.homeroom_teacher_id`에 둡니다(한 학급 = 한 담임).

### 2.4 시정표 (★ 핵심)

```sql
CREATE TABLE bell_schedules (         -- 시정표 프로필
  id INTEGER PRIMARY KEY,
  term_id INTEGER NOT NULL REFERENCES terms(id) ON DELETE CASCADE,
  name    TEXT NOT NULL,              -- '저학년 시정표'
  note    TEXT
);

CREATE TABLE bell_slots (             -- 프로필 × 요일 × 슬롯
  id INTEGER PRIMARY KEY,
  bell_schedule_id INTEGER NOT NULL REFERENCES bell_schedules(id) ON DELETE CASCADE,
  day_of_week INTEGER NOT NULL,       -- 1=월 … 5=금
  slot_type   TEXT    NOT NULL,       -- PERIOD | LUNCH | OTHER
  period_no   INTEGER,                -- PERIOD일 때만 값 존재
  label       TEXT    NOT NULL,       -- '1교시' | '점심'
  start_min   INTEGER NOT NULL,       -- ★ 자정 기준 분 (09:00 → 540)
  end_min     INTEGER NOT NULL,
  CHECK (start_min BETWEEN 0 AND 1439),
  CHECK (end_min > start_min),
  CHECK ((slot_type =  'PERIOD' AND period_no IS NOT NULL)
      OR (slot_type <> 'PERIOD' AND period_no IS NULL))
);
CREATE UNIQUE INDEX ux_bell_period
  ON bell_slots(bell_schedule_id, day_of_week, period_no) WHERE period_no IS NOT NULL;
CREATE INDEX ix_bell_day ON bell_slots(bell_schedule_id, day_of_week);

CREATE TABLE grade_bell_map (         -- 학년 → 프로필
  term_id INTEGER NOT NULL REFERENCES terms(id) ON DELETE CASCADE,
  grade   INTEGER NOT NULL,
  bell_schedule_id INTEGER NOT NULL REFERENCES bell_schedules(id),
  PRIMARY KEY (term_id, grade)
);
```

- **"수요일 1학년은 4교시까지"** = 저학년 프로필 day=3에 period_no 1~4 행만 존재
- **"점심시간이 학년마다 다름"** = 프로필마다 `slot_type='LUNCH'` 행의 시각이 다름
- 저장 단위는 프로필이지만 **화면은 학년 기준**으로 보여줍니다(§5.3)

### 2.5 수업 시간표

```sql
CREATE TABLE lessons (                -- 전담 수업 (+ 교차 지도 수업)
  id INTEGER PRIMARY KEY,
  term_id     INTEGER NOT NULL REFERENCES terms(id) ON DELETE CASCADE,
  teacher_id  INTEGER NOT NULL REFERENCES teachers(id) ON DELETE CASCADE,
  day_of_week INTEGER NOT NULL,
  period_no   INTEGER NOT NULL,
  class_id    INTEGER NOT NULL REFERENCES classes(id) ON DELETE CASCADE,
  subject_id  INTEGER REFERENCES subjects(id),
  note        TEXT,
  UNIQUE (term_id, teacher_id, day_of_week, period_no)  -- 한 교사가 동시에 두 수업 불가
);
CREATE INDEX ix_lesson_class   ON lessons(term_id, class_id, day_of_week, period_no);
CREATE INDEX ix_lesson_teacher ON lessons(term_id, teacher_id, day_of_week);
```

- 한 학급에 같은 교시에 두 전담이 들어가는 것은 **DB 제약이 아니라 화면 경고**로 처리합니다
  (합반·교차 지도 등 예외 운영이 실제로 존재하므로 저장 자체를 막지 않음)
- 이 수업의 **실제 시각**은 `class_id → grade → grade_bell_map → bell_slots(day, period)`로
  조회합니다. 즉 "5학년 6교시 수업"이면 5학년 시정표의 6교시 시각을 씁니다

```sql
CREATE TABLE teacher_busy_blocks (    -- 수업 외 필수 일정
  id INTEGER PRIMARY KEY,
  term_id    INTEGER REFERENCES terms(id) ON DELETE CASCADE,
  teacher_id INTEGER NOT NULL REFERENCES teachers(id) ON DELETE CASCADE,
  recurrence TEXT NOT NULL,           -- WEEKLY | ONCE
  day_of_week   INTEGER,              -- WEEKLY일 때
  specific_date TEXT,                 -- ONCE일 때 (YYYY-MM-DD)
  start_min  INTEGER NOT NULL,
  end_min    INTEGER NOT NULL,
  label      TEXT NOT NULL,           -- '업무 협의회', '순회 지도'
  blocks_substitution INTEGER NOT NULL DEFAULT 1
);
```

### 2.6 결근 / 보결

```sql
CREATE TABLE absence_reasons (        -- 연가/병가/공가/출장/연수/기타
  code TEXT PRIMARY KEY, label TEXT NOT NULL,
  sort_order INTEGER NOT NULL, is_builtin INTEGER NOT NULL DEFAULT 1
);

CREATE TABLE absences (               -- 교사 부재
  id INTEGER PRIMARY KEY,
  teacher_id INTEGER NOT NULL REFERENCES teachers(id),
  date       TEXT NOT NULL,
  is_all_day INTEGER NOT NULL DEFAULT 1,
  start_min  INTEGER, end_min INTEGER,     -- 반일/시간 단위일 때
  reason_code TEXT REFERENCES absence_reasons(code),
  reason_text TEXT,
  status     TEXT NOT NULL DEFAULT 'ACTIVE',   -- ACTIVE | CANCELLED
  created_at TEXT NOT NULL
);
CREATE INDEX ix_absence_date ON absences(date, status);

CREATE TABLE substitutions (          -- ★ 보결 배정 기록
  id INTEGER PRIMARY KEY,
  term_id     INTEGER REFERENCES terms(id),
  absence_id  INTEGER REFERENCES absences(id),   -- 일괄 보결 묶음(선택)
  date        TEXT    NOT NULL,       -- YYYY-MM-DD
  day_of_week INTEGER NOT NULL,
  -- 대상 (스냅샷: 학급 정보가 바뀌어도 기록은 불변)
  class_id    INTEGER REFERENCES classes(id),
  grade       INTEGER NOT NULL,
  class_no    INTEGER NOT NULL,
  -- 시간 (스냅샷: 시정표가 바뀌어도 기록은 불변)
  slot_type   TEXT    NOT NULL,       -- PERIOD | LUNCH
  period_no   INTEGER,
  slot_label  TEXT    NOT NULL,       -- '6교시' | '점심'
  start_min   INTEGER NOT NULL,
  end_min     INTEGER NOT NULL,
  -- 교사
  absent_teacher_id INTEGER REFERENCES teachers(id),
  sub_teacher_id    INTEGER NOT NULL REFERENCES teachers(id),
  subject_id        INTEGER REFERENCES subjects(id),
  -- 사유 / 상태
  reason_code TEXT, reason_text TEXT,
  status      TEXT NOT NULL DEFAULT 'ASSIGNED',  -- ASSIGNED | CANCELLED
  -- 감사 정보 (추천 품질 점검용)
  recommend_rank   INTEGER,           -- 추천 몇 순위를 선택했는지
  recommend_reason TEXT,              -- '동학년, 수업 종료'
  created_at   TEXT NOT NULL,
  cancelled_at TEXT, cancel_reason TEXT
);
CREATE INDEX ix_sub_date  ON substitutions(date, status);
CREATE INDEX ix_sub_subt  ON substitutions(sub_teacher_id, date, status);
CREATE INDEX ix_sub_month ON substitutions(substr(date,1,7), status);
```

**횟수 집계는 항상 `status='ASSIGNED'`만 셉니다. 취소 기록은 물리 삭제하지 않습니다.**
조회만 한 경우에는 아무 행도 생기지 않으므로 횟수에 영향이 없습니다.

### 2.7 배정 기준 (룰 설정)

```sql
CREATE TABLE exclusion_rules (        -- STEP1 후보 필터링
  rule_key    TEXT PRIMARY KEY,
  enabled     INTEGER NOT NULL DEFAULT 1,
  params_json TEXT NOT NULL DEFAULT '{}'
);

CREATE TABLE priority_rules (         -- STEP2 추천 정렬
  rule_key    TEXT PRIMARY KEY,
  enabled     INTEGER NOT NULL DEFAULT 0,
  sort_order  INTEGER NOT NULL,       -- 1순위, 2순위, …
  params_json TEXT NOT NULL DEFAULT '{}'
);
```

룰의 **정의(라벨·설명·비교 함수)는 코드에**, **사용 여부와 순서만 DB에** 둡니다.
새 기준을 추가할 때 DB 스키마 변경이 필요 없습니다.

### 2.8 마이그레이션

`PRAGMA user_version`을 버전 번호로 사용하고 `migrations/NNN_*.sql`을 번호순으로 적용합니다.
앱 시작 시 자동 실행하며, 마이그레이션 전에 DB 파일을 `backups/`로 자동 복사합니다.

```
PRAGMA foreign_keys = ON;
PRAGMA journal_mode = WAL;
```

---

## 3. 보결 가능 여부 판단 로직 (STEP 1)

### 3.1 시간 표현

```rust
/// 자정 기준 분. 09:00 → 540
pub type Min = i32;

#[derive(Clone, Copy, PartialEq, Eq, Debug)]
pub struct Interval { pub start: Min, pub end: Min }   // [start, end)

impl Interval {
    /// 1분이라도 겹치면 true. 접점(09:40 종료 ↔ 09:40 시작)은 겹치지 않음
    pub fn overlaps(&self, o: &Interval) -> bool {
        self.start < o.end && o.start < self.end
    }
}
```

**접점 처리 규칙**: 1교시가 09:40에 끝나고 2교시가 09:40에 시작하면 겹치지 않은 것으로 봅니다.
반열린 구간 `[start, end)`을 쓰면 자동으로 해결됩니다.

### 3.2 교사별 일정(BusyBlock) 생성

특정 **날짜 D**에 대해 각 교사가 바쁜 시간 구간 목록을 만듭니다.

```rust
pub enum BusyKind {
    Lesson,          // 전담/교차 수업
    HomeroomLesson,  // 담임 파생 수업
    Lunch,           // 자기 학년 점심 지도
    FixedDuty,       // 등록된 고정 업무
    Substitution,    // 이미 배정된 보결
    Absence,         // 결근 / 출장
}

pub struct BusyBlock {
    pub teacher_id: i64,
    pub interval:   Interval,
    pub kind:       BusyKind,
    pub label:      String,   // '5-2 영어', '3학년 점심', '이미 보결(4-1 3교시)'
}
```

생성 절차 (요일 W = D의 요일):

1. **직접 수업** — `lessons` where `day_of_week = W`
   → 각 행마다 `class.grade`의 시정표에서 `(W, period_no)` 슬롯을 찾아 `Interval` 획득
2. **담임 파생 수업** (`derive_homeroom_schedule = true`일 때)
   - 각 학급 C: C의 학년 시정표에서 `(W, slot_type='PERIOD')`인 모든 슬롯을 가져옴
   - 그중 `lessons`에 `(C, W, period)` 행이 있는 교시는 **제외** (그 시간은 전담이 담당)
   - 남은 교시 = C의 담임교사가 수업 중인 구간
3. **점심 지도** (`exclude_homeroom_on_own_lunch = true`일 때)
   - 담임교사에게 자기 학급 학년의 `slot_type='LUNCH'` 슬롯을 `Lunch` 블록으로 추가
4. **고정 업무** — `teacher_busy_blocks` where (`WEEKLY` and `day=W`) or (`ONCE` and `date=D`)
5. **기존 보결** — `substitutions` where `date=D` and `status='ASSIGNED'`
   → `sub_teacher_id`에게 `[start_min, end_min)` 블록 추가
6. **결근** — `absences` where `date=D` and `status='ACTIVE'`
   → 종일이면 `[0,1440)`, 시간 단위면 해당 구간

### 3.3 요청 시간 해석

사용자가 고르는 것: **날짜 / 학년 / 반 / 교시(또는 점심)**

```
날짜 2026-09-09 → 요일 = 수(3)
학년 5          → grade_bell_map → '고학년 시정표'
(수, 6교시)     → bell_slots → start_min=830(13:50), end_min=870(14:30)

⇒ 요청 구간 = Interval { start: 830, end: 870 }
```

해당 슬롯이 없으면(예: 수요일 1학년 6교시) 조회를 막고
"1학년은 수요일에 5교시까지 수업합니다. [시정표 설정으로 이동]"을 안내합니다.

### 3.4 후보 필터링 룰 (확장 지점 ①)

```rust
pub struct ExclusionCtx<'a> {
    pub target:       Interval,        // 보결이 필요한 시간
    pub target_class: ClassInfo,       // 대상 학급
    pub date:         NaiveDate,
    pub absent_teacher_id: Option<i64>,
    pub busy:     &'a BusyIndex,       // teacher_id → Vec<BusyBlock>
    pub settings: &'a Settings,
}

pub struct Exclusion { pub rule_key: &'static str, pub message: String }

pub trait ExclusionRule: Send + Sync {
    fn key(&self)   -> &'static str;
    fn label(&self) -> &'static str;
    /// 제외 사유를 반환하면 후보에서 탈락
    fn check(&self, t: &TeacherInfo, ctx: &ExclusionCtx) -> Option<Exclusion>;
}
```

기본 내장 룰:

| rule_key | 내용 | 기본 |
|---|---|---|
| `NOT_SUBSTITUTABLE` | `is_substitutable = 0` | 항상 켬(끌 수 없음) |
| `IS_ABSENT_TEACHER` | 결근 교사 본인 | 항상 켬 |
| `TIME_CONFLICT` | BusyBlock과 1분이라도 겹침 | 항상 켬 |
| `ALREADY_ASSIGNED` | 같은 시간에 이미 다른 보결 | TIME_CONFLICT에 포함, 사유만 구분 표시 |
| `SAME_GRADE_ONLY` | 동학년만 허용 | **꺼짐** (요구사항대로 기본 제한 없음) |
| `MAX_PER_DAY` | 하루 N회 초과 제외 (`params: {max: 2}`) | 꺼짐 |
| `MAX_CONSECUTIVE` | 연속 N교시 초과 제외 | 꺼짐 |

**새 제외 조건 추가 = `ExclusionRule` 구현체 하나 작성 + 레지스트리 등록.** 스키마 변경 없음.

### 3.5 요구사항 시나리오 검증

| 상황 | 처리 |
|---|---|
| 학년별 교시 시간이 다름 | 각 수업의 시각을 **해당 학급 학년의 시정표**에서 조회하므로 자동 해결 |
| 저학년 수업이 일찍 끝남 | 2학년 담임의 마지막 블록이 12:40 종료 → 5학년 6교시(13:50~14:30)와 안 겹침 → **후보** |
| 저·고학년 점심시간이 다름 | 1학년 점심 11:50~12:40, 5학년 점심 13:00~13:50이 각각 별도 블록 |
| 전담교사 공강 | `lessons`에 행이 없는 교시 = 블록 없음 → **후보** |
| 점심 보결 | 요청 구간이 `slot_type='LUNCH'` 슬롯. 나머지 로직 동일 |
| 이미 보결 배정된 교사 | `Substitution` 블록으로 겹침 → 제외 |
| 저학년 담임 → 고학년 보결 | 학년 제한 룰이 기본 꺼짐 → **가능** |

### 3.6 "현재 상태" 라벨 계산

```
그 날 그 교사의 모든 BusyBlock을 시간순 정렬한 뒤,
  - 요청 시간 이후에 남은 블록이 없다 → "수업 종료"
  - 요청 시간 이전에도 블록이 없다   → "수업 없음"
  - 그 외                            → "공강"
```

---

## 4. 추천 우선순위 정렬 로직 (STEP 2)

### 4.1 룰 인터페이스 (확장 지점 ②)

```rust
pub struct Candidate {
    pub teacher: TeacherInfo,
    pub status:  TeacherStatus,      // 수업종료 | 공강 | 수업없음
    pub count_today: i32,
    pub count_month: i32,
    pub count_total: i32,
    pub homeroom_grade: Option<i32>, // 담임인 경우
    pub homeroom_class: Option<i32>,
    pub subjects: Vec<String>,
    pub last_period_end: Option<Min>,
}

pub trait PriorityRule: Send + Sync {
    fn key(&self)   -> &'static str;
    fn label(&self) -> &'static str;   // 설정 화면에 보일 한국어 이름
    fn hint(&self)  -> &'static str;   // 한 줄 설명

    /// Less = a가 더 앞순위
    fn compare(&self, a: &Candidate, b: &Candidate, ctx: &PriorityCtx) -> Ordering;

    /// 이 룰의 관점에서 이 후보에게 붙일 근거 태그. 해당 없으면 None
    fn tag(&self, c: &Candidate, ctx: &PriorityCtx) -> Option<String>;
}
```

### 4.2 정렬 실행

```rust
let rules: Vec<&dyn PriorityRule> = registry.enabled_in_order(); // DB sort_order 순

candidates.sort_by(|a, b| {
    for r in &rules {
        match r.compare(a, b, &ctx) {
            Ordering::Equal => continue,
            other => return other,
        }
    }
    // 최종 동점 처리: 누적 적은 순 → 이름 가나다순 (결과가 항상 동일하도록 결정적으로)
    a.count_total.cmp(&b.count_total).then(a.teacher.name.cmp(&b.teacher.name))
});
```

**추천 근거 문구**는 두 가지를 합쳐 만듭니다.

1. 활성화된 룰 중 이 후보에게 `tag()`가 붙는 것들 → `동학년`, `전담`, `수업 종료`
2. 바로 아래 순위와 순서를 가른 룰 → 툴팁으로 `"2위보다 이번 달 보결이 1회 적음"`

### 4.3 내장 룰 목록

| rule_key | 라벨 | 비교 기준 | 태그 |
|---|---|---|---|
| `SAME_GRADE` | 동학년 교사 우선 | 담임 학년 == 대상 학년 → 앞 | `동학년` |
| `FEWEST_TODAY` | 당일 보결 적은 순 | `count_today` 오름차순 | `오늘 0회` |
| `FEWEST_MONTH` | 이번 달 보결 적은 순 | `count_month` 오름차순 | — |
| `FEWEST_TOTAL` | 누적 보결 적은 순 | `count_total` 오름차순 | — |
| `PREFER_SPECIAL` | 전담교사 우선 | `role=SPECIAL` → 앞 | `전담` |
| `PREFER_FINISHED` | 수업 종료 교사 우선 | `status=수업종료` → 앞 | `수업 종료` |
| `PREFER_LOWER_GRADE` | 저학년 담임 우선 | 담임 학년 오름차순 | `저학년` |
| `SAME_SUBJECT` | 같은 과목 담당 우선 (확장) | 결시 과목 담당 → 앞 | `과목 일치` |
| `PREFER_ADJACENT` | 인접 교시 근무 중 우선 (확장) | 직전/직후 교시 수업 → 앞 | `연속 교시` |

기본 프리셋(설치 직후): `SAME_GRADE` → `FEWEST_TODAY` → `FEWEST_TOTAL`
설정 화면에서 켜기/끄기 + Drag & Drop 순서 변경 + [기본값으로 되돌리기]

### 4.4 설정 화면의 즉시 미리보기

우선순위를 바꾸면 화면 아래에 **가장 최근 조회 조건으로 재정렬한 결과**를 바로 보여줍니다.
"1순위를 바꾸면 추천이 이렇게 달라집니다"를 눈으로 확인할 수 있게 하여,
설명 없이도 기준을 이해할 수 있게 합니다.

### 4.5 보결 횟수 계산 정의

```sql
-- 오늘
SELECT COUNT(*) FROM substitutions
 WHERE sub_teacher_id = ? AND date = ? AND status = 'ASSIGNED';
-- 이번 달
 WHERE sub_teacher_id = ? AND substr(date,1,7) = ? AND status = 'ASSIGNED';
-- 누적 (전체 / 학기 기준을 설정에서 선택)
 WHERE sub_teacher_id = ? AND status = 'ASSIGNED' [AND term_id = ?];
```

한 번의 조회에서 후보 전원의 세 카운트를 **쿼리 3회로 일괄 집계**한 뒤 메모리에서 매칭합니다
(교사 수 × 3 쿼리 방지).

---

## 5. 화면 / 메뉴 구조

### 5.1 라우팅

```
/welcome                     최초 실행 화면
/setup/school                1. 학교 기본 설정
/setup/bell                  2. 시정표·점심시간
/setup/teachers              3. 교사 등록
/setup/lessons               4. 전담 시간표
/setup/priority              5. 보결 배정 우선순위
/setup/done                  6. 설정 완료

/  → /find                   ★ 메인
/find                        보결 조회
/assignments                 배정 내역
/stats                       보결 현황
/teachers                    교사 관리
/timetable                   시간표 관리 (시정표 + 전담 시간표 탭)
/settings                    설정
```

**앱 시작 시 분기**

```
setup_completed_at 있음  → /find
setup_steps에 진행 흔적  → "설정을 이어서 진행할까요? 마지막 작업: 교사 등록"
                            [이어서 설정하기] [처음부터 다시]
아무 흔적 없음           → /welcome
```

### 5.2 보결 조회 (`/find`) — 가장 많이 쓰는 화면

```
┌──────────────────────────────────────────────────────────────┐
│  보결 조회                                                    │
│  ┌── 조회 조건 카드 ──────────────────────────────────────┐  │
│  │  날짜 [2026-09-09 ▾]  수요일     빠른 선택 [오늘][내일] │  │
│  │  학년 [5학년 ▾]   반 [2반 ▾]                            │  │
│  │  시간 [1][2][3][4][5][6][점심]  ← 없는 교시는 비활성    │  │
│  │        선택 시 아래에 "13:50 ~ 14:30" 즉시 표시          │  │
│  │  결근교사 [김OO ▾](선택)   사유 [연가 ▾]                │  │
│  │                                  [ 보결 가능 교사 조회 ] │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                               │
│  5학년 2반 · 수요일 6교시 · 13:50~14:30 | 가능 7명 / 제외 12명 │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 순위 교사  구분 담당  현재상태   오늘 이번달 누적 추천근거│  │
│  │  ①  김OO  담임 5-1  수업 종료     0    1    3  [동학년] │  │
│  │  ②  박OO  전담 체육  공강         0    2    4  [전담]   │  │
│  │  ③  이OO  담임 2-1  수업 종료     0    2    5  [수업종료]│  │
│  │  ▸ 배정할 수 없는 교사 12명 보기 (사유 표시)             │  │
│  └────────────────────────────────────────────────────────┘  │
│                                   [ 선택한 교사로 보결 배정 ] │
└──────────────────────────────────────────────────────────────┘
```

- 1·2·3위는 순위 배지 색으로 구분(1위 연두, 2·3위 하늘, 이하 회색)
- 행 클릭 = 선택. **더블클릭으로 즉시 배정되지 않게** 하고 하단 버튼을 눌러야 확정
- 결과가 0명이면: "이 시간에 배정 가능한 교사가 없습니다" + 제외 사유 요약 +
  [제외된 교사 보기] / [우선순위 설정 확인]
- 배정 성공 시 토스트 + "이어서 다른 시간 배정하기"(같은 날짜·학급 유지) 버튼

### 5.3 시정표 설정 화면

프로필 저장 구조를 사용자에게 노출하지 않고 **학년 탭**으로 보여줍니다.

```
[1학년][2학년][3학년][4학년][5학년][6학년]     1학년 시정표는
                                             [2학년]과 같음 ⓘ
 구분    월       화       수       목       금
 1교시  09:00   09:00   09:00   09:00   09:00
        ~09:40  ~09:40  ~09:40  ~09:40  ~09:40
 …
 점심   11:50   11:50   11:50   11:50   11:50
 5교시  12:40   12:40    —      12:40   12:40   ← '—' = 그 날 없음
 6교시   —       —       —       —       —

 [+ 교시 추가]  [이 학년 시정표를 다른 학년에도 적용 ▾]
```

- 한 학년을 수정하면 같은 프로필을 쓰는 학년에 함께 반영되며, 그 사실을 상단에 표시
- 다르게 하려면 [이 학년만 다르게 설정] → 프로필 자동 복제·분리
- 첫 진입 시 "1~2 / 3~4 / 5~6학년" 3개 프로필 템플릿 또는 전 학년 동일 중 선택
- 시각은 `09:00` 마스크 입력 + 상하 화살표. 교시 겹침은 저장 전 경고

#### 시각 길이의 출처는 한 곳뿐이다 (v0.1.5)

이 화면에는 길이를 다루는 곳이 셋 있습니다 — **처음부터 다시 만들기**,
**시간 일괄 조정**, 그리고 표 아래의 **[중간놀이 추가] · [점심시간 추가] · [교시 추가]**.
처음에는 세 곳이 각자 기본값을 들고 있었습니다. 그래서 일괄 조정에서
중간놀이를 20분으로 바꿔도 [중간놀이 추가]는 자기 기본값인 30분을 넣었습니다.

지금은 `bellModel.ts` 의 `Lengths` 하나를 세 곳이 함께 씁니다.

| 무엇 | 어디 |
|---|---|
| 형태와 기본값 | `Lengths` · `DEFAULT_LENGTHS` |
| 자료에서 읽어 내기 | `detectLengths(rows)` — 없는 값은 `null` |
| 둘을 합치기 | `mergeLengths(rows, remembered)` |
| 지금 값을 들고 있는 곳 | `BellEditor` 의 `useRemembered('bell.lengths')` 하나 |

- `addPeriod` · `addLunch` · `addRecess` 는 길이를 **필수 인자로** 받습니다.
  기본값을 두면 부르는 쪽이 넘기는 것을 잊어도 조용히 다른 값이 들어갑니다.
- 시정표를 **새로 불러올 때 한 번** 자료에 든 길이를 받아들이고
  (`mergeLengths`), 그 뒤 사용자가 고친 값은 자료가 덮어쓰지 않습니다.
  그래서 아직 저장하지 않은 입력이 화면이 새로 그려질 때 되돌아가지 않습니다.
- 값은 브라우저 저장소에 남으므로 설정 단계를 옮기거나 앱을 다시 켜도 유지됩니다.
  자료 파일에는 들어가지 않습니다.

첫 진입 시 **처음부터 다시 만들기**는 펼쳐 두고 **시간 일괄 조정**은 접어 둡니다.
전자는 시정표를 처음 만드는 사람이 먼저 볼 곳이고, 후자는 이미 만들어 둔 것을
손볼 때 쓰는 곳입니다.

### 5.4 전담 시간표 화면

```
전담교사           주간 시간표                    [붙여넣기] [지우기]
┌─────────┐  ┌──────────────────────────────────────────┐
│ ● 김OO  │  │ 교시   월      화      수      목     금  │
│   체육  │  │ 1교시 3-1 영어  —    4-1 영어   —     —   │
│ ○ 이OO  │  │ 2교시 3-2 영어 4-1영어  —      —     —   │
│   영어  │  │ 3교시  —     4-2 영어 3-1 영어  —     —   │
│ ○ 박OO  │  │ …                                        │
│   과학  │  └──────────────────────────────────────────┘
└─────────┘   칸 클릭 → [학년▾][반▾][과목▾] 인라인 편집
              ⚠ 3-1반 수요일 1교시에 다른 전담(이OO)도 배정됨
```

- 좌측 목록에 교사별 **입력된 수업 시수**를 표시 → 누락 발견이 쉬움
- 붙여넣기(Phase 10): 클립보드 TSV/HTML 표 파싱 → **미리보기에서 확인·수정 후 [적용]**

### 5.4-A 보결 수당 화면 (v0.1.6)

`보결 배정 → 기록 생성 → 여기서 집계` 한 방향으로만 흐르는 **후처리** 화면입니다.
후보 판정(`domain::find`)과 배정(`repo::assign`)은 수당을 모릅니다.

```
[월별] [기간 지정]   ‹ 2026년 9월 ›  [이번 달]           [엑셀 파일로 저장]
기간 2026년 9월 · 지급 기준 본인 발생분 차감 · 1회 15,000원  설정에서 바꾸기

 지급 대상 5명 | 총 보결 22회 | 총 지급 인정 18회 | ★ 총 지급액 270,000원

 교사   구분   보결한 횟수  본인 발생 보결  지급 인정  1회 수당   지급액
 김민수 담임        7            2           5      15,000원   75,000원  [상세]
 박지현 전담        4            0           4      15,000원   60,000원  [상세]
 합계                22           4          18              270,000원
```

#### 계산 근거는 `substitutions` 하나뿐이다

| 숫자 | 어떻게 세는가 |
|---|---|
| 보결한 횟수 | `sub_teacher_id` 가 그 사람인 `status='ASSIGNED'` 건수 |
| 본인 발생 보결 | `absent_teacher_id` 가 그 사람인 `status='ASSIGNED'` 건수 |

**`absences` 를 세지 않는 것이 중요합니다.** 결근으로 수업이 비었더라도 아무도
배정되지 않았다면 기록이 남지 않으므로, 그런 시간은 차감에 들어가지 않습니다.
취소분(`CANCELLED`)은 어느 쪽에도 들어가지 않으니, 취소한 뒤 다른 사람에게 다시
배정한 시간도 한 번만 세어집니다.

#### 계산 결과를 저장하지 않는다

수당표를 DB에 두지 않습니다. 1회 금액을 15,000원에서 20,000원으로 바꾸면 다음
조회에서 곧바로 20,000원으로 나옵니다. 통계와 같은 방침입니다 — 집계표를 따로
두면 반드시 원본과 어긋나는 날이 오고, 학교 업무에서 숫자가 어긋나는 것은
없느니만 못합니다.

지급 확정·마감 스냅샷은 **일부러 만들지 않았습니다.** 나중에 "9월 지급자료는 당시
15,000원 기준으로 확정해 보존해야 한다"는 요구가 생기면, 계산(`domain::pay::settle`)
을 그대로 두고 **결과를 떠 담는 기능**을 따로 얹을 수 있습니다. 그래서 계산은 화면에
묶여 있지 않습니다.

#### 정책은 코드로 늘린다

`PayRule` 구현체 하나가 정책 하나입니다 (추천 기준의 `PriorityRule` 과 같은 구조).
DB에는 정책 **코드 문자열**만 저장하므로, 새 정책을 추가할 때 스키마도 화면도
손대지 않습니다.

| 코드 | 지급 인정 횟수 |
|---|---|
| `ALL_ASSIGNED` | 보결한 횟수 |
| `DEDUCT_OWN_CAUSED` | `max(0, 보결한 횟수 - 본인 발생 보결)` |

정책은 횟수만 받지 않고 **사례 목록 전체**(`Tally::cases`)를 받습니다. 앞으로 나올 수
있는 '월 최대 지급 횟수', '1일 최대 지급 횟수', '특정 보결 유형 제외' 같은 규정은
날짜와 유형을 봐야 계산되기 때문입니다. 다만 **요청하지 않은 규칙은 만들어 두지
않았습니다.**

#### 그 밖에

- 학급 표기는 `substitutions.class_label` 스냅샷을 씁니다. 반 이름을 바꿔도 지난
  기록의 표기가 달라지지 않습니다.
- 기간이 지금 학기를 벗어나면 알려 주기만 하고 막지 않습니다. 학기가 바뀐 뒤 지난
  달 수당을 정산하는 일이 실제로 있습니다.
- 설정 초기화(동작 옵션 되돌리기)는 수당 금액을 건드리지 않습니다. 판단 옵션을
  되돌리려던 사람이 학교가 정한 금액까지 0원으로 잃는 것은 놀라운 일입니다.
- 내보낸 파일의 금액은 **숫자**입니다. 쉼표와 '원'은 엑셀 숫자 서식(`#,##0"원"`)으로
  보여 주는 것이라서, 그대로 합계를 낼 수 있습니다 (§5.6).

### 5.6 내보내기 — 엑셀 파일(XLSX) (v0.1.6)

내보내기는 세 곳에 있고, **모두 같은 유틸리티 하나**를 씁니다.

| 화면 | 명령 | 파일 | 시트 |
|---|---|---|---|
| 배정 내역 | `export_history_xlsx` | `보결배정내역-<시각>.xlsx` | 배정 내역 · 조회 조건 |
| 보결 현황 | `export_stats_xlsx` | `보결현황-<시각>.xlsx` | 요약 · 교사별 · 결근 · 날짜별 · 미배정 · 조회 조건 |
| 보결 수당 | `export_pay_xlsx` | `보결수당-<시각>.xlsx` | 교사별 수당 · 조회 조건 |

#### 왜 CSV를 그만두었나

CSV는 글자만 담습니다. 그래서

- 금액과 횟수가 결국 **글자**였습니다. `15,000` 처럼 쉼표를 붙이면 엑셀이 글자로
  읽어 합계를 못 냈고, 쉼표를 떼면 사람이 읽기 어려웠습니다.
- 한글이 깨지지 않게 파일 앞에 **BOM 세 바이트**를 붙여야 했습니다.
- '보결 현황' 표 네 개를 한 파일에 넣으려면 **빈 줄로 나누는** 수밖에 없었습니다.

XLSX에서는 세 가지가 형식 안에서 해결됩니다. 숫자는 숫자로 저장되고, 글자는
UTF-8 XML이라 장치가 필요 없고, 표는 시트로 나눕니다.

#### 서식은 `repo::xlsx` 한 곳에만 있다

만드는 쪽은 **무슨 값인지만** 말하고, 서식은 `Cell` 의 종류가 정합니다.
그래서 세 화면의 금액이 서로 다른 모양으로 나올 수 없습니다.

| `Cell` | 저장 | 엑셀 서식 |
|---|---|---|
| `Text` | 글자 | — |
| `Count(n)` | 숫자 | `#,##0` |
| `Money(n)` | 숫자 | `#,##0"원"` |
| `Date("YYYY-MM-DD")` | 실제 날짜(일련번호) | `yyyy-mm-dd` |
| `DateTime("... HH:MM:SS")` | 실제 날짜+시각 | `yyyy-mm-dd hh:mm` |
| `Time(자정 기준 분)` | 실제 시각(하루의 비율) | `hh:mm` |
| `Blank` | 빈 칸 | — |

날짜·시각을 실제 값으로 담으므로 엑셀에서 **정렬과 기간 필터가 제대로** 됩니다.
해석할 수 없는 값(예: `2026-13-99`)은 글자로 남깁니다 — 내보내기가 자료 때문에
실패하면 안 됩니다. `24:00` 도 글자로 둡니다(실제 시각으로 담으면 `00:00` 이 되어
하루가 사라집니다).

#### 모든 시트가 같은 모양이다

- 1행은 **굵은 머리글**(연회색 배경 + 아래 실선)
- 그 행이 **고정**되어 스크롤해도 열 이름이 보인다
- 자료 시트에는 **자동필터**. 합계 행은 필터 범위에서 빼서, 걸러도 늘 보인다
- 열 너비는 만드는 쪽이 정한다 (한글은 두 칸을 차지한다고 보고 넉넉히)

#### 조회 조건은 시트를 따로 만든다

어느 기간·어느 기준으로 뽑았는지 파일만 보고 알 수 있어야 합니다. 자료 시트
위에 조건을 끼워 넣으면 머리글이 1행이 아니게 되어 고정·필터가 어긋나므로,
**'조회 조건' 시트**를 따로 둡니다 (여기에는 필터를 걸지 않습니다).

#### 파일 이름

기존 이름을 그대로 두고 확장자만 바꿨습니다 — `보결현황-20260907-200057.xlsx`.

### 5.4-B 전담교사 식사시간 (v0.1.8)

전담교사도 밥을 먹는다. 그 시간에는 **일반 수업 보결**을 맡길 수 없다.
그런데 그 시간이 학교 전체에 하나로 정해지지 않는다 — 그 날 어느 학년
수업에 들어갔느냐로 달라진다.

```
            저학년            고학년
 점심     12:10~13:10       13:10~14:00
 5교시    13:10~13:50       12:20~13:00
```

고학년 5교시(12:20~13:00)를 하면 저학년 점심과 겹치므로 늦게 먹고,
저학년 5교시(13:10~13:50)를 하면 반대가 된다.

#### 저학년·고학년을 코드에 박지 않는다

학교마다 학년 묶음과 점심시간이 다르다. 그래서 **학년별 시정표의 점심
구간**(`bell_slots` 의 `LUNCH`)에서 패턴을 뽑아 쓴다. 실제 우리 학교 자료에는
세 가지가 있었다 — 3·4학년 11:25~12:15 / 1·2학년 12:10~13:00 /
5·6학년 12:55~13:45. 두 가지를 가정한 코드였다면 틀렸을 것이다.

#### 정하는 순서

위가 항상 이긴다 (`domain/meal.rs`).

| 순서 | 무엇 | 표시 |
|---|---|---|
| 1 | 교사별·요일별 직접 지정 | 직접 지정 |
| 2 | 그 날 수업과 겹치지 않는 시간이 **하나뿐**이면 그것 | 자동 |
| 3 | 여러 개면 **학교 기본 식사시간** (그것이 수업과 겹치면 가능한 것으로 대체) | 학교 기본 |
| 4 | 정할 수 없으면 | 확인 필요 |

겹침은 프로그램 전체가 쓰는 규칙 하나(`Interval::overlaps`)를 그대로 쓴다.
그래서 수업 12:20~13:00 과 식사 13:00~14:00 은 겹치지 않는다.

#### 모르면 빼지 않는다

'확인 필요'일 때는 **후보에서 빼지 않고** 보결 조회 화면에 왜 그런지 알려
준다. 모르면서 빼면 배정할 사람이 사라지고 이유도 알 수 없다. 설정이
없다는 이유로 점심시간을 추측하지도 않는다.

#### 점심 보결에는 걸리지 않는다 — 가장 중요한 예외

전담교사는 **자기 식사시간에도 점심 보결은 맡을 수 있다.** 그래서 이 제약을
`BusyBlock` 으로 만들지 않았다 — 그렇게 하면 점심 보결까지 막힌다.
판정하는 쪽(`domain/find.rs`)이 대상이 일반 수업인지 점심인지 보고 적용한다.

```
if is_general_period && t.role_code == ROLE_SPECIAL {
    if 식사시간과 겹치면 → EXCLUDED_SPECIAL_MEAL ("식사시간 13:10~14:00")
}
```

추천 순서(`PriorityRule`)와는 분리되어 있다. 점수를 낮추는 것이 아니라
**맡을 수 없는 시간**으로 본다.

#### 담임 점심 규칙과 다른 것이다

담임의 급식 지도는 `exclude_homeroom_on_own_lunch` 설정으로 예전부터 있었고
그대로다. 이번 것은 전담교사의 요일별 식사시간이라는 별개 개념이다.
겹침 함수만 함께 쓰고 정책은 나눠 두었다.

#### 계산해 둔 값을 남기지 않는다

저장하는 것은 **사람이 정한 두 가지**뿐이다.

| 무엇 | 어디 | 왜 |
|---|---|---|
| 학교 기본 식사시간 | `settings.meal_default_bell_id` | 시각이 아니라 *어느 시정표의 점심시간에 함께 먹는가*. 시정표를 고치면 따라 바뀌고, 요일마다 다른 학교도 맞는다 |
| 교사별·요일별 직접 지정 | `teacher_meal_overrides` 표 | 교사 × 요일이라 관계형이 맞고, 교사를 지우면 함께 사라진다 |

자동 판정 결과는 저장하지 않는다. 시정표가 바뀌었는데 옛 계산이 남아 잘못
판정하는 일이 없다.

#### 직접 지정도 아무 시간이나 받지 않는다

그 날 실제 수업과 겹치는 시간은 저장을 막는다 —
"그 시간에는 실제 수업(12:20~13:00)이 있어 식사시간으로 지정할 수 없습니다."

### 5.5 나머지 화면

| 화면 | 내용 |
|---|---|
| 배정 내역 | 기간·학급·교사 필터, 상태 뱃지(배정됨/취소됨), [배정 취소](확인창 + 사유), 엑셀 내보내기 |
| 보결 현황 | 교사별 오늘/이번 달/누적 표 + 월 선택 + 막대 그래프. 편중 상·하위 강조 |
| 교사 관리 | 목록 + 추가/수정. `보결 배정 대상` 토글을 목록에서 바로 조작. 삭제 대신 `비활성` |
| 시간표 관리 | 시정표 탭 / 전담 시간표 탭 (설정 마법사와 동일 컴포넌트 재사용) |
| 설정 | 학교 정보 · 배정 우선순위 · 동작 옵션 · 백업/복원 · 업데이트 확인 · 데이터 초기화 |

### 5.6 디자인 토큰

```css
:root {
  --sky-50:#F0F9FF; --sky-100:#E0F2FE; --sky-300:#7DD3FC;
  --sky-500:#38BDF8; --sky-600:#0EA5E9; --sky-700:#0284C7;   /* Primary */
  --lime-100:#ECFCCB; --lime-400:#A3E635; --lime-500:#84CC16; /* Accent  */
  --gray-50:#F8FAFC; --gray-200:#E2E8F0;
  --gray-500:#64748B; --gray-700:#334155; --gray-900:#0F172A; /* Text    */
  --warn:#F59E0B; --danger:#EF4444;

  --radius-card:14px; --radius-btn:10px;
  --shadow-card:0 1px 3px rgba(15,23,42,.06), 0 1px 2px rgba(15,23,42,.04);
  --font:'Pretendard', -apple-system, 'Malgun Gothic', sans-serif;
}
```

- 배경 흰색, 카드 위 옅은 하늘색 구분선. 그라데이션·그림자 과용 금지
- 1순위 배지·[배정] 버튼·"가능" 상태에만 연두색 사용(남용하면 강조가 사라짐)
- 기본 폰트 15px, 표 14px. 12px 이하 금지. 최소 창 크기 1100 × 720

---

## 6. IPC 명령 목록 (초안)

```
setup_get_state()                        → { steps, completed }
setup_save_step(step_key, payload)
school_get() / school_save(dto)

bell_list_profiles() / bell_get(grade)
bell_save_profile(dto) / bell_split_grade(grade)
bell_apply_to_grades(profile_id, grades[])

teacher_list(filter) / teacher_upsert(dto) / teacher_set_active(id, bool)
subject_list() / subject_upsert(dto)
class_list() / class_upsert(dto) / class_set_homeroom(class_id, teacher_id)

lesson_get_by_teacher(teacher_id)        → 주간 격자
lesson_upsert(dto) / lesson_delete(id)
lesson_bulk_replace(teacher_id, rows[])  ← 붙여넣기 적용
lesson_validate()                        → 경고 목록(중복/누락)

find_candidates(FindRequest)             → ★ { slot, eligible[], excluded[] }
sub_assign(AssignRequest)                → substitution_id
sub_cancel(id, reason)
sub_list(filter) / sub_get(id)
absence_upsert(dto) / absence_cancel(id)

stats_summary(range)                     → 교사별 오늘/월/누적
priority_list() / priority_save(rules[]) / priority_reset()
exclusion_list() / exclusion_save(rules[])
settings_get() / settings_set(key, value)

backup_export(path) / backup_import(path)
update_check() / update_install()
```

에러는 Rust에서 `AppError { code, user_message, detail }`로 통일하고,
화면에는 `user_message`(예: "5학년 수요일 6교시 시간이 설정되어 있지 않습니다")만 보여주며
`detail`은 [자세히] 접기 안에 둡니다.

---

## 7. 개발 단계별 계획

각 Phase는 **끝났을 때 `npm run tauri dev`로 실제 실행되고 그 단계 기능을 손으로 써볼 수 있는 상태**를
완료 조건으로 삼습니다.

### Phase 0 — 개발 환경 (선행 필요)

- 이 PC에 **Rust 툴체인이 설치되어 있지 않습니다.** `rustup` + MSVC Build Tools 설치 필요
- WebView2 런타임 확인 (Windows 11은 기본 포함)
- 완료 기준: `cargo --version` 동작

### Phase 1 — 프로젝트 골격 + DB + 기본 UI

- Tauri 2 + Vite + React + TS 초기화, 디자인 토큰, 공용 컴포넌트, 사이드바 레이아웃
- rusqlite 연결, 마이그레이션 러너, `001_init.sql` 전체 스키마, 기본 데이터 시드
- IPC 왕복 확인용 `app_info()` 명령
- 완료 기준: 앱 실행 → 사이드바 화면 표시, `%APPDATA%`에 DB 파일 생성 확인

### Phase 2 — 최초 실행 + 설정 마법사 뼈대

- Welcome 화면, Progress Stepper, 6단계 라우팅, 단계별 저장/이어하기
- 완료 기준: 1단계 입력 → 앱 종료 → 재실행 시 "이어서 설정하기"

### Phase 3 — 시정표 / 점심시간

- 프로필 템플릿 선택, 학년 탭 편집기, 시각 입력 검증, 학년 매핑/분리
- 완료 기준: 학년별로 다른 점심시간 저장 후 재조회 정확

### Phase 4 — 교사 관리

- 교사 CRUD, 구분, 담임 학급 지정, 과목 다중 지정, 보결 대상 토글, 비활성 처리
- 완료 기준: 20명 등록·수정·검색이 막힘 없이 동작

### Phase 5 — 전담교사 시간표

- 교사 선택 → 주간 격자 → 칸 인라인 편집, 중복 경고, 시수 표시
- 완료 기준: 전담 3명 입력 후 담임 파생 시간표가 올바른지 진단 화면에서 확인

### Phase 6 — 보결 가능 교사 조회 엔진 ★

- `domain/time.rs`, `schedule.rs`, `exclusion.rs` + **cargo 단위 테스트**
- `find_candidates` 명령, 조회 화면(정렬은 이름순 임시)
- 완료 기준: §3.5의 7개 시나리오가 전부 테스트로 통과

### Phase 7 — 추천 우선순위 엔진

- `domain/priority.rs` 룰 레지스트리, 설정 화면(Drag & Drop), 추천 근거, 즉시 미리보기
- 완료 기준: 동일 데이터에서 우선순위 구성만 바꿔 추천 순서가 의도대로 변함

### Phase 8 — 배정 / 취소 / 결근

- 배정 저장(스냅샷), 취소(상태 전환 + 확인창), 결근 등록, 하루 일괄 보결
- 완료 기준: 배정 → 같은 시간 재조회 시 그 교사 제외 / 취소 시 다시 후보 복귀

### Phase 9 — 현황 및 통계

- 교사별 오늘/월/누적, 월 선택, 그래프, 엑셀 내보내기
- 완료 기준: 취소분이 집계에서 빠지는지 확인

### Phase 10 — 입력 편의 기능

- Excel/HWP 표 붙여넣기 파서(TSV + HTML table), 미리보기·수정 후 적용
- 시정표 일괄 복사, 교사 명단 붙여넣기
- 완료 기준: 실제 엑셀 시간표 1개를 붙여넣어 수정 3회 이내로 완성

### Phase 11 — 자동 업데이트

- `tauri-plugin-updater`, 서명 키 생성(비공개 키는 GitHub Secrets), `latest.json` 발행
- GitHub Actions: 태그 push → 빌드 → 서명 → Release 업로드
- 마지막 확인 시각 저장 후 24시간 경과 시에만 조회, 설정에 [업데이트 확인]
- 완료 기준: v0.1.0 → v0.1.1 실제 업데이트 성공

### Phase 12 — 배포

- NSIS 설치파일(한국어), 아이콘, 앱 이름, 백업/복원, 사용 설명 1장, 데이터 초기화
- 완료 기준: 다른 PC에서 설치 → 설정 마법사 → 보결 배정까지 완주

---

## 8. 확장성 요약

**시간 충돌 판단**

- 모든 시간은 정수 분. 비교는 `Interval::overlaps` 한 곳에서만 수행
- 바쁜 시간의 출처는 `BusyKind`로 구분되지만 형태는 모두 `BusyBlock`으로 동일
  → 새로운 종류의 일정(방과후, 순회, 겸임교 근무 등)이 생겨도
  `BusyBlock` 생성기 하나만 추가하면 충돌 판단 로직은 손대지 않음
- 제외 조건은 `ExclusionRule` 트레이트 구현 + 등록만으로 추가

**학교별 추천 기준**

- 룰의 정의는 코드, 사용 여부·순서·파라미터는 DB
- 새 기준 추가 = `PriorityRule` 구현 + 레지스트리 등록. 스키마 변경 없음
- 기존 학교의 설정은 `rule_key` 기준으로 보존되므로 업데이트 후에도 그대로 동작
